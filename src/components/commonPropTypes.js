import { string, boolean, object } from 'prop-types'

export default {
  heights: string,
  layout: string,
  media: string,
  noloading: boolean,
  on: string,
  sizes: string,
  width: string,
  height: string,
  fallback: object
}
